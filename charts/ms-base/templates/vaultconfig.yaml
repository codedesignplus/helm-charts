{{- if and (.Values.vault.create) (.Values.vault.server) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ms-base.name" . }}-vaultserver
data:
  server: {{ .Values.vault.server }}
{{- end -}}