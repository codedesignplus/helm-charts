{{- if and .Values.vault.create .Values.vault.token -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-vaultsecret
type: Opaque
data:
  token: {{ .Values.vault.token | b64enc | quote }}
{{- end -}}